unbind C-b
set -g prefix C-space

set -g mode-keys vi

bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

bind -r C-h resize-pane -L 5
bind -r C-j resize-pane -D 5
bind -r C-k resize-pane -U 5
bind -r C-l resize-pane -R 5

unbind v
bind v split-window -h -c '#{pane_current_path}'
unbind s
bind s split-window -v -c '#{pane_current_path}'

bind -r '<' swap-window -d -t '{previous}' # Move window left.
bind -r '>' swap-window -d -t '{next}' # Move window right.

bind "'" choose-session

bind Space last-window
bind r next-layout

# Move current pane
bind K swap-pane -D
bind J swap-pane -U

bind -T copy-mode-vi _ send-keys -X back-to-indentation

# Mouse
set -g mouse on
# bind -T MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel 'xsel -ib'

# vim:ft=tmux
